---
title: 纯 Zig 的 IETF QUIC + HTTP/3
description: 纯 Zig 实现的 IETF QUIC 传输与 HTTP/3 协议栈
template: splash
hero:
  title: 纯 Zig 的 QUIC + HTTP/3
  tagline: |
    从零用 Zig 编写的 IETF QUIC + HTTP/3 协议栈 —— QUIC v1 与 v2、纯 Zig TLS 1.3
    （含 0-RTT 与后量子密钥交换）、完整 HTTP/3 / QPACK / WebTransport、异步 std.Io
    运行时，已与 quic-go、quiche、s2n-quic 完成线路级互通验证。
  actions:
    - text: 快速开始
      link: /zh-cn/quick-start/
      icon: right-arrow
      variant: primary
    - text: 功能对比
      link: /zh-cn/comparisons/
      icon: document
      variant: minimal
    - text: GitHub
      link: https://github.com/venjiang/quicz
      icon: github
      variant: minimal
---

## 线上验证，不只是单元测试

quicz 的握手与 stream 路径由真实 UDP 探针覆盖：进程内 loopback、独立进程 Zig，以及
启用证书校验的 Go / Rust 对端。

```sh
$ examples/interop/run_external_interop.sh all
# 对 quic-go (Go)、quiche (Rust)、s2n-quic (Rust) 的证书校验互通：
# 握手 + 传输，stream 0/4 上带 FIN 的 echo —— 全程开启证书校验。
```

## quicz 目前已实现

<div className="qz-split">
<div>

- **纯 Zig TLS 1.3 + 0-RTT** —— 无 C 密码学；ECDSA P-256、X25519、X25519Kyber768（后量子）、AES-128/256-GCM、ChaCha20-Poly1305、会话恢复。
- **QUIC v1 & v2 transport** —— stream、flow control、连接迁移、path validation、Retry、stateless reset、密钥更新、版本协商、DATAGRAM、multipath、ECN、PMTUD、GSO/GRO、连接池、qlog。
- **Loss recovery + NewReno / CUBIC** —— 报文 pacing、HyStart++、PTO jitter、快速重传、app-limited 检测。
- **异步 I/O 运行时** —— 事件驱动 `std.Io` server/client，每连接独立 handler（std.http 模型）。
- **HTTP/3** —— 完整连接管理、SETTINGS、GOAWAY、stream 状态机、QPACK 静态 + 动态表。
- **WebTransport**（完整会话管理）**+ HTTP Datagrams** (RFC 9297) **+ 流重置部分交付**。
- **线路级互通验证**：quic-go、quiche、s2n-quic。

</div>
<div>

```zig
var conn = try quicz.Connection.init(
    std.heap.page_allocator, .client, .{
    .initial_max_data = 65_536,
    .initial_max_stream_data = 65_536,
    .initial_max_streams_bidi = 16,
});
defer conn.deinit();

const id = try conn.openStream();
try conn.sendOnStream(id, "hello", true);

var buf: [1350]u8 = undefined;
const frame = (try conn.pollTx(0, &buf))
    orelse return error.NoPendingFrame;
```

</div>
</div>

推荐入口是高层 `Endpoint → Connection → Stream` API；上面这段是低层 packet API。见
[快速开始](/zh-cn/quick-start/)。

## 互操作矩阵

| 对端 | 语言 / 协议栈 | 探针 |
| --- | --- | --- |
| quic-go | Go | 启用证书校验的 echo client/server + `run_external_interop.sh` |
| quiche | Rust | 启用证书校验的握手 + 传输 |
| s2n-quic | Rust | 启用证书校验的握手 + 传输 |
| QUIC-Interop-Runner | — | handshake、transfer、retry 自测 |

自己跑一遍：[示例指南](/zh-cn/examples/)。

## quicz 横向对比

权威功能矩阵在四个协议栈上追踪 41 项能力，覆盖率汇总如下：

| 指标 | quic-go | quiche | s2n-quic | quicz |
| --- | --- | --- | --- | --- |
| 传输层（19 项） | 19/19 | 14/19 | 14/19 | 19/19 |
| 拥塞控制（8 项） | 6/8 | 6/8 | 7/8 | 7/8 |
| 密码套件（5 项） | 5/5 | 5/5 | 5/5 | 5/5 |
| 应用层（6 项） | 6/6 | 3/6 | 0/6 | 6/6 |
| 平台（3 项） | 2/3 | 0/3 | 1/3 | 1/3 |
| **合计（41 项）** | **38/41** | **28/41** | **27/41** | **40/41** |

quicz 覆盖全部传输、密码套件与应用层能力（40/41）；缺口仅为 BBR（主动移除）与平台
特有的 FIPS / XDP。完整 41 行逐项对比见[功能对比](/zh-cn/comparisons/)。

## 性能

<div className="qz-stats">
  <div className="qz-stat"><div className="num">~390<small> MB/s</small></div><div className="lbl">单流 · 真实握手</div></div>
  <div className="qz-stat"><div className="num">21.7<small> μs</small></div><div className="lbl">回声延迟 · P50</div></div>
  <div className="qz-stat"><div className="num">~304<small> MB/s</small></div><div className="lbl">4 流聚合</div></div>
</div>

secnetperf 风格的 macOS loopback 微基准，**真实 TLS 1.3 握手**（无 GSO / XDP）；跨实现数字仅为指示性参考；installed-keys（旁路握手）上限约 1.94 GB/s。完整吞吐图、延迟百分位、丢包恢复与方法见[性能](/zh-cn/performance/)页。

## 不在范围内

- **BBR** —— 2026-08 主动移除，改用 CUBIC。
- **FIPS 140-3** —— 仅 quic-go（Go 1.26+）。
- **XDP 零拷贝 I/O** —— 仅 s2n-quic。

权威状态与验收证据见[传输任务矩阵](https://github.com/venjiang/quicz/blob/main/docs/zh-CN/quic_transport_tasks.md)。
API 趋于稳定但仍在演进。

## 接下来

- [快速开始](/zh-cn/quick-start/) —— 用 `zig fetch` 加入 quicz，跑起第一个端点。
- [功能对比](/zh-cn/comparisons/) —— quicz vs quic-go / quiche / s2n-quic，41 行。
- [性能](/zh-cn/performance/) —— 吞吐、延迟、丢包恢复、CPU 开销。
- [架构](/zh-cn/architecture/) —— endpoint、connection 与 TLS 层如何组合。
- [规范覆盖](/zh-cn/spec/) —— RFC 9000/9001/9002 逐节状态。
- [示例](/zh-cn/examples/) —— echo、DATAGRAM、后量子、0-RTT 与互通探针。
- [安全](/zh-cn/security/) —— 信任边界与范围内攻击的防御。
